# TOUCHMAP — File ownership per task.
# Maps tasks to the files they are expected to touch.
# AI agents check this before editing to stay in scope.
updated_utc: 2026-04-18T00:00:00Z

tasks:
  SM.INIT.AUTO_START:
    files:
      - server.js
      - services.json
      - package.json
      - service-install.js
      - service-uninstall.js
      - tray.ps1
      - launch-tray.vbs
      - launch-server.vbs
    notes: "Auto-starting silent service setup — launch-server.vbs added for silent server startup via Windows Startup folder"

  META.AUDIT:
    files:
      - ops/TOUCHMAP.yaml
      - ops/ROUTER.yaml
      - ops/NEXT.yaml
      - masterroutetable.md
      - Prompt.md
      - docs/architecture-bible/01-system-overview.md
    notes: "Codespace audit — meta-router and docs sync"
