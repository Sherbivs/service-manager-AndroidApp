title: Service Manager AndroidApp — Next Task Pointer
version: 1
last_updated: "2026-04-22T00:00:00Z"

queue:
  - id: SMA.001
    title: Network Layer Foundation
    status: READY
    priority: P0
    description: >
      Create data/ApiService.kt (Retrofit interface), data/ServiceRepository.kt,
      data/RetrofitClient.kt. All 7 API endpoints. Base URL read from
      EncryptedSharedPreferences. Unit tests with mocked responses.
    acceptance_criteria_ref: "Tasklist.md#SMA.001"

  - id: SMA.007
    title: Network Security Config (LAN HTTP support)
    status: READY
    priority: P0
    description: >
      Create res/xml/network_security_config.xml with domain exception for
      user-configured LAN host. Reference from AndroidManifest.xml.
      Do NOT set usesCleartextTraffic=true globally.
    acceptance_criteria_ref: "Tasklist.md#SMA.007"

  - id: SMA.002
    title: Server URL Onboarding / Settings Screen
    status: BLOCKED
    blocked_by: [SMA.001]
    priority: P0
    description: >
      First-run onboarding screen and settings screen for configuring
      the Service Manager server URL. Store via EncryptedSharedPreferences.

notes: >
  Bootstrap complete as of 2026-04-22. No application source code written yet.
  Begin with SMA.001 (network layer) and SMA.007 (network security config) in parallel.
  Both are independent P0 tasks.
