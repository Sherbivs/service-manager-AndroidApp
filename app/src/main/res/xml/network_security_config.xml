<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Config — Service Manager Android App

    Cleartext is denied by default and enabled only for explicit LAN/dev hosts.
    This prevents insecure global cleartext while preserving local development access.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Canonical Service Manager LAN and routed hosts -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.23.106</domain>
        <domain includeSubdomains="true">sensaimanager.drip</domain>
        <domain includeSubdomains="true">tcb.drip</domain>
        <domain includeSubdomains="true">blt.drip</domain>
        <domain includeSubdomains="true">sensailinuxserver.drip</domain>
    </domain-config>

    <!-- Local emulator/device loopback testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
